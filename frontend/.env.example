# API base URL used by the frontend fetch client.
# Default (/api) works with both the Vite dev proxy and the Docker nginx proxy.
# Override when the API lives at a different origin.
VITE_API_BASE_URL=/api
